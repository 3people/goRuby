#include <stdio.h>

int main(int argc, char const *argv[])
{
    int n;
    scanf("%d", &n);
    if (n == 0)
    {
        printf("YONSEI");
    }
    else
    {
        printf("Leading the Way to the Future");
    }
}
